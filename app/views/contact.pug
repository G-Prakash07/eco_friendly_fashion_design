doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Contact Us - Fashion Design Eco-friendly
    link(rel="stylesheet", href="/contact.css")
  body
    include navbar.pug

    section.contact-hero
      h1 Contact Us
      p We’d love to hear from you—just drop your name, email, and message below.

    section.contact-form
      form(method="POST", action="/submit-contact")
        .form-group
          label(for="name") Name:
          input(type="text", id="name", name="name", required=true)

        .form-group
          label(for="email") Email:
          input(type="email", id="email", name="email", required=true)

        .form-group
          label(for="message") Message:
          textarea(id="message", name="message", rows="4", required=true)

        button(type="submit") Submit

    footer
      p © 2025 Fashion Design Eco-friendly | All rights reserved
